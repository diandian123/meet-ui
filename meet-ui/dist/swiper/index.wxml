<view class="mt-swiper {{'mt-swiper--' + type}} {{extClass}}" style="height: {{height}}rpx;"><swiper bindchange="handleChange" bindanimationfinish="handleAnimationfinish" bindtransition="handleTransition" previous-margin="{{type === 'card' ? '1rpx' : 0}}" next-margin="{{type === 'card' ? '1rpx' : 0}}" current="{{current}}" vertical="{{vertical}}" interval="{{interval}}" duration="{{duration}}" circular="{{circular}}" autoplay="{{autoplay}}" easing-function="{{easingFunction}}" class="mt-swiper__items"><swiper-item wx:for="{{items}}" wx:key="index" bindtap="handleTap" data-index="{{index}}" class="mt-swiper-item {{activeIndex === index ? 'mt-swiper--active' :''}}"><image mode="aspectFill" src="{{item.image}}" class="mt-swiper__image" style="width: {{type !== 'card' ? (width+'rpx') : (100+'%')}}; border-radius: {{radius}}rpx;"/><block wx:if="{{type !== 'card' }}"><view wx:if="{{hasTitle && item.title}}" class="mt-swiper__title" style="left: {{(750 - width) / 2}}rpx; right: {{(750 - width) / 2}}rpx; border-radius: {{radius}}rpx;">{{item.title}}</view></block></swiper-item></swiper><view wx:if="{{indicatorDots}}" class="mt-swiper__dots {{vertical ? 'mt-swiper__dots--vertical' : ''}}" style="right: {{(750 - width) / 2 + 20}}rpx;"><view wx:for="{{items}}" wx:key="index" class="mt-swiepr__dot {{activeIndex === index ? 'mt-swiepr__dot--active' : ''}}" style="background-color: {{activeIndex === index? indicatorActiveColor : indicatorColor}};"></view></view></view>