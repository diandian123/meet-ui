<view id="dragBox" class="mt-imagepicker mt-imagepicker--{{type}}" style="grid-template-columns: repeat({{column}}, 1fr);"><view wx:for="{{itemList}}" wx:key="src" class="mt-imagepicker__item"><block hidden="{{!ready}}"><view wx:if="{{item.src}}" class="mt-imagepicker__item-bd {{draging ? 'item__draging' : ''}} {{startIndex === index ? 'item__actived' : ''}} {{item.deleted ? 'item__hidden' : ''}}" data-index="{{index}}" catchlongpress="{{sortable ? 'handleLongPress' : ''}}" catchtouchmove="{{touchMove}}" catchtouchcancel="{{sortable ? 'handleTouchend' : ''}}" catchtouchend="{{sortable ? 'handleTouchend' : ''}}" style="width: {{item.boxWidth}}px; height: {{item.boxHeight}}px; left:{{item.left}}px; top:{{item.top}}px;"><image hidden="{{!item.boxWidth}}" mode="{{imageMode}}" src="{{item.src}}" data-index="{{index}}" bindtap="previewImage" class="mt-imagepicker__item-image"></image><view wx:if="{{type==='normal'}}" hidden="{{!item.boxWidth}}" data-index="{{index}}" bindtap="handleDelete" class="mt-imagepicker__item__del"></view></view><view wx:elif="{{item.type === 'upload'}}" bindtap="handleChoose" class="mt-imagepicker__item-bd {{draging ? 'draging' : ''}}" style="width: {{item.boxWidth}}px; height: {{item.boxHeight}}px; left:{{item.left}}px; top:{{item.top}}px;"><mt-icon hidden="{{!item.boxWidth}}" name="mt-icon--plus" size="70" color="#b6b5b6" class="mt-imagepicker__item__add"></mt-icon></view></block></view></view><canvas canvas-id="compressCanvas" style="width:{{canvasWidth}}px; height:{{canvasHeight}}px; position:absolute; top:-2000%;"></canvas>