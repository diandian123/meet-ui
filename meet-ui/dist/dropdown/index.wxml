<view class="mt-dropdown {{ activeIndex===-1 ?'':'mt-dropdown--active' }}"><block wx:if="{{title.length>0}}"><view wx:for="{{title}}" class="mt-dropdown__title {{disabled ? 'mt-dropdown__title--disable': ''}} {{activeIndex !== -1?'mt-dropdown__title--active':''}} mt-dropdown__title--{{position}}" wx:key="index" bindtap="showPopup" data-index="{{index}}" catchtouchmove="{{activeIndex===-1 ?'':'stopEvent'}}"><view class="mt-dropdown__title__text {{activeIndex === index? 'title--active': ''}}">{{item}}<mt-icon name="mt-icon--left" class="{{activeIndex === index ? 'mt-icon--up' : 'mt-icon--down'}}"></mt-icon></view></view></block><block wx:else><slot bindtap="showPopup" catchtouchmove="stopEvent" data-index="{{0}}" name="title"></slot></block><block wx:for="{{formatValue}}" wx:key="index"><menu wx:if="{{type !== 'custom'}}" class="mt-dropdown__menu" bindresetActive="resetActive" bindclose="handleClose" bindsetTitle="setTitle" bindclick="handleClick" items="{{item}}" scene="{{scene}}" changeTitle="{{changeTitle}}" bottomDistance="{{bottomDistance}}" topClosable="{{topClosable}}" type="{{ type}}" current="{{formatcurrent[index] || []}}"></menu><menu wx:else class="mt-dropdown__menu" scene="{{scene}}" bottomDistance="{{bottomDistance}}" bindclose="handleClose" bindresetActive="resetActive" bindclick="handleClick" topClosable="{{topClosable}}" type="custom"><slot name="item--{{index}}"></slot></menu></block></view>