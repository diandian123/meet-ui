<view class="mt-switch-bar"><view class="mt-switch-bar__main"><text wx:for="{{items}}" wx:key="index" bindtap="handleChange" data-index="{{index}}" class="mt-switch-bar__item {{current === index ? 'mt-switch-bar__item--active' : ''}} {{item.hasNew ? 'mt-switch-bar__item--new' : ''}}" style="width: {{itemWidth}}rpx;">{{item.label}}</text><view class="mt-switch-bar__focus" style="width: {{itemWidth}}rpx; transform: translateX({{current * itemWidth}}rpx);"></view></view></view>