<view class="mt-navbar {{'mt-navbar--' + platform}} {{'mt-navbar--' + mode}}" style="padding-top: {{statusBarHeight}}px; {{mode === 'static' ? 'background:' + background : ''}} {{opacity ? 'background:' +  newProps.background + '; opacity:' + opacity : ''}}"><view class="mt-navbar__bd" style="height:{{navBarInnerHeight}}px; padding:0 {{navBarColumnGap}}px; color: {{opacity ? newProps.textColor: textColor}};"><view wx:if="{{type === 'back'}}" bindtap="handleBack" class="mt-navbar__back"><mt-icon name="mt-icon--left" size="56"></mt-icon></view><view wx:elif="{{type === 'home'}}" bindtap="handleHome" class="mt-navbar__home"><mt-icon name="mt-icon--home" size="42"></mt-icon></view><view class="mt-navbar__title__wp" style="{{titlePosition ===  'center' ? 'text-align: center' : ''}}"><view wx:if="{{opacity}}" class="mt-navbar__title {{platform === 'android' && type !== 'normal' && titlePosition === 'auto' ? 'mt-navbar__title--sp' : ''}}" style="width: {{platform === 'android' && !titlePosition ===  'center' ? androidTitleWidth : iosTitleWidth}}px; line-height: {{navBarInnerHeight}}px;">{{newProps.title}}</view><view wx:elif="{{title}}" class="mt-navbar__title {{platform === 'android' && type !== 'normal' && titlePosition === 'auto' ? 'mt-navbar__title--sp' : ''}}" style="width: {{platform === 'android'&& !titlePosition ===  'center' ? androidTitleWidth : iosTitleWidth}}px; line-height: {{navBarInnerHeight}}px;">{{title}}</view><slot></slot></view></view><view style="height: 4px;"></view></view>