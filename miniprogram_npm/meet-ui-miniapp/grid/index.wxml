<view class="mt-grid {{radius ? 'mt-grid--radius' : ''}} {{'mt-grid--' + direction}} {{'mt-grid--' + bdType}}" style="background: {{bgColor}}; border-radius: {{radius}}rpx;"><block wx:if="{{title}}"><view class="mt-grid__title">{{title}}</view></block><view class="mt-grid__main"><view wx:for="{{items}}" wx:key="index" bindtap="handleTap" data-index="{{index}}" class="mt-grid__item {{bdType === 'none' || bdType === 'innerRow' || index % column === column - 1 ? '' : 'mt-grid__item--rb'}} {{bdType !== 'none' && index < itemsLen - (itemsLen % column || column) ? 'mt-grid__item--bb':''}}" style="width:{{(100 / column) + '%'}};"><block wx:if="{{type === 'preset'}}"><image wx:if="{{item.badge === undefined}}" mode="aspectFill" src="{{item.icon}}" class="mt-grid__item-icon" style="width: {{iconWidth}}rpx; height: {{iconHeight || iconWidth}}rpx; border-radius: {{iconRadius}}rpx;"></image><mt-badge wx:else position="top" value="{{item.badge}}"><image mode="aspectFill" src="{{item.icon}}" class="mt-grid__item-icon" style="width: {{iconWidth}}rpx; height: {{iconHeight || iconWidth}}rpx; border-radius: {{iconRadius}}rpx;"></image></mt-badge><view class="mt-grid__item-text" style="font-size: {{titleSize}}rpx;">{{item.title}}</view><text wx:if="{{item.desc}}" class="mt-grid__item-desc">{{item.desc}}</text></block><block wx:elif="{{type === 'scope'}}"><grid-item item="{{item}}" class="mt-grid__scope"></grid-item></block><block wx:elif="{{type === 'slot'}}"><slot name="item-{{index}}"></slot></block></view></view></view>