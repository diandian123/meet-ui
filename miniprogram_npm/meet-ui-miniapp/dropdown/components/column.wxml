<view class="mt-mask" wx:if="{{maskVisible||visible}}" catchtouchmove="stopEvent" id="mask" bindtap="maskClose"></view><view class="mt-dropdown__menu" style="{{visible ? menuStyle:''}}"><mt-popup hasTopClose="{{false}}" hasRadius="{{false}}" type="dropdown" visible="{{popupTarget}}" position="{{position}}" class="mt-dropdown__popup" bindclose="close"><scroll-view class="mt-dropdown__item" wx:if="{{visible}}" scroll-y><block wx:if="{{type === 'custom' || columns.length>0}}"><mt-cell-group clear wx:if="{{type === 'radio' || type ==='relation'}}"><mt-cell wx:for="{{columns}}" wx:key="index" hasArrow="{{false}}" data-index="{{index}}" data-value="{{item.value}}" bindtap="handleRadio" class="{{item.checked?'menu--active':''}}">{{item.label}}<mt-icon wx:if="{{item.checked}}" slot="right" name="mt-icon--success-b" class="mt-icon--success"></mt-icon></mt-cell></mt-cell-group><view wx:if="{{type === 'checkbox'}}" class="mt-dropdown__menu__checkbox"><mt-checkbox-group bindchange="handleCheckbox" type="button" items="{{columns}}" column="2"></mt-checkbox-group><view class="mt-dropdown__check__button"><mt-button class="mt-dropdown__check__button--rest" type="plain" height="78" disabled="{{selectCurrent.length === 0}}" bindtap="reset">取消</mt-button><mt-button class="mt-dropdown__check__button--confrim" height="78" disabled="{{selectCurrent.length === 0}}" bindtap="confrim">确认</mt-button></view></view><block wx:if="{{type === 'custom'}}"><slot/></block></block><view wx:else class="mt-dropdown__menu__nodate">- 暂无数据 -</view></scroll-view></mt-popup></view>